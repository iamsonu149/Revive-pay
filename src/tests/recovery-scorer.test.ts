import {describe,it,expect} from 'vitest'; import {scoreRecovery} from '../domain/recovery/recovery-scorer'; const base={failureReason:'BANK_TECHNICAL',amount:5000,retryCount:0,bankHealthScore:90,recentSuccessfulPayments:6,paymentMethodAgeDays:100,customerEngagementScore:80,contactCountLast7Days:0,attemptedAt:new Date()}; describe('scorer',()=>{it('retries healthy technical failure',()=>expect(scoreRecovery(base).recommendation).toBe('RETRY_LATER'));it('requests update for expiry',()=>expect(scoreRecovery({...base,failureReason:'EXPIRED_CARD'}).recommendation).toBe('SEND_PAYMENT_UPDATE_LINK'));it('stops contact cap',()=>expect(scoreRecovery({...base,contactCountLast7Days:2}).recommendation).toBe('STOP'));it('requires high value approval',()=>expect(scoreRecovery({...base,amount:10001}).requiresHumanApproval).toBe(true));it('stops cancellation',()=>expect(scoreRecovery({...base,failureReason:'CANCELLED_SUBSCRIPTION'}).recommendation).toBe('STOP'));});
